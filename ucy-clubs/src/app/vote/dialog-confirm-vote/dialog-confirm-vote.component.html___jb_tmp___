<h1 mat-dialog-title>{{data.name}}, you have votes are:</h1>
<div mat-dialog-content *ngFor="let v of data.vote">
  {{v.name }}: {{v.availDays.toString()}}


</div>
  <div mat-dialog-actions>
  <button mat-button (click)="onNoClick()">Cancel</button>
  <button mat-button >Submit vote</button>
  </div>
